/*
$.fn.dropdownHover = function(){
	return this.each(function(){
		var $el = $(this);
		$el
			.on('click.dropdown.data-api', function(e){ e.stopImmediatePropagation() })
			.on('mouseenter mouseleave', function(){ $el.dropdown('toggle') })
			.next()
			.on('mouseenter mouseleave', function(){ $el.dropdown('toggle') })
	})
};*/
